# Copy to .env.local and fill in. All env is read through the central config module — never process.env ad hoc.

# Contact form delivery (Resend)
RESEND_API_KEY=
RESEND_EMAIL_TO=
RESEND_EMAIL_FROM=

# AI features (AMA chat + blog TL;DRs)
ANTHROPIC_API_KEY=
