# Implementation Features

Granular, implementable feature plans. Each doc covers one deliverable, named `F###-<kebab-slug>.md` with monotonic numbering. TDD applies: **no feature is `ready` without test cases.**

Created only after the release's FSD is locked and the relevant tech discovery is `complete`. Format and lifecycle: see the `feature-implementation` skill (`.claude/skills/feature-implementation/`).
