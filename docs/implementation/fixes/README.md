# Fix Records

Bug-fix records, named `X###-<kebab-slug>.md` with monotonic numbering. Same frontmatter and lifecycle as features, with `type: fix`. A fix may skip product/tech discovery and start directly from a `docs/known-issues.md` entry.

Format: see the `feature-implementation` skill (`.claude/skills/feature-implementation/`).
